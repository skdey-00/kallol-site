import type { Metadata } from "next"

export const metadata = {
  title: "Volunteer at Kallol | Kallol",
  description: "Volunteer with Kallol and help run the pujas, camps and community services at the Kallol campus in Goregaon West.",
}

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}

import type { Metadata } from "next"

export const metadata = {
  title: "About Kallol, Bengali Community & Cultural Organization | Kallol",
  description: "The story of Kallol, a Bengali social and cultural organization in Goregaon West, Mumbai, grown around the Kallol Kali Mandir since its founding.",
}

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}

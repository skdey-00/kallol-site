import type { Metadata } from "next"

export const metadata = {
  title: "Donate to a Puja | Kallol",
  description: "Contribute to a specific puja or offering at Kallol Kali Mandir, Goregaon West, Mumbai.",
}

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}

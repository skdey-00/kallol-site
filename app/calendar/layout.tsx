import type { Metadata } from "next"

export const metadata = {
  title: "Puja Calendar — Bengali Year 1433 | Kallol Kali Mandir",
  description: "Month-by-month calendar of pujas and events at Kallol Kali Mandir, Bangur Nagar, Goregaon West, Mumbai.",
}

export default function PageLayout({ children }: { children: React.ReactNode }) {
  return children
}
